# 主要都市ジャンプ機能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** サイドバーのドロップダウンから主要 12 都市を選ぶと、地図がその都市中心へ flyTo で移動し、既存の bounds 連動で周辺 Y 字路が自動ロードされる。

**Architecture:** 固定の都市配列（`cities.ts`）→ `CityJumpSelect`（optgroup 付き select）で選択 → `App` が `jumpTarget` state を更新 → `MapView` 内の `CityFlyTo`（`useMap`）が `map.flyTo` を実行。バックエンド変更なし。

**Tech Stack:** TypeScript / React 19 / react-leaflet / Leaflet / Vitest / @testing-library/react

## Global Constraints

- バックエンド（API・SQL・スキーマ）は変更しない。
- 既存の URL 直リンク（`#node=...`）と `FocusAnimation` は変更しない。
- 全都市共通ズーム定数 `CITY_JUMP_ZOOM = 14`。
- flyTo オプション: `{ animate: true, duration: 2 }`。
- コンポーネントは既存流儀に合わせ `memo(function Name() {...})` で定義。
- 設計ドキュメント: `docs/superpowers/specs/2026-08-08-city-jump-design.md`。

## File Structure

- Create `frontend/src/data/cities.ts` — `City` 型と `CITIES` 配列（12 エントリ）。唯一の都市データソース。
- Create `frontend/src/data/cities.test.ts` — `CITIES` の妥当性検証。
- Create `frontend/src/components/CityJumpSelect.tsx` — サイドバーの select UI。
- Create `frontend/src/components/CityJumpSelect.test.tsx` — 選択挙動テスト。
- Modify `frontend/src/components/MapView.tsx` — `jumpTarget` prop と `CityFlyTo` 追加。
- Modify `frontend/src/App.tsx` — `jumpTarget` state、`CityJumpSelect` 配置、`MapView` へ prop 渡し。

---

### Task 1: 都市データ (`cities.ts`)

**Files:**
- Create: `frontend/src/data/cities.ts`
- Test: `frontend/src/data/cities.test.ts`

**Interfaces:**
- Consumes: なし
- Produces:
  - `export interface City { name: string; country: string; lat: number; lon: number }`
  - `export const CITIES: City[]`（12 エントリ）

- [ ] **Step 1: Write the failing test**

`frontend/src/data/cities.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { CITIES } from './cities';

describe('CITIES', () => {
  it('12 都市を含む', () => {
    expect(CITIES).toHaveLength(12);
  });

  it('各エントリの座標が有効範囲内', () => {
    for (const c of CITIES) {
      expect(c.lat).toBeGreaterThanOrEqual(-90);
      expect(c.lat).toBeLessThanOrEqual(90);
      expect(c.lon).toBeGreaterThanOrEqual(-180);
      expect(c.lon).toBeLessThanOrEqual(180);
    }
  });

  it('name と country が非空', () => {
    for (const c of CITIES) {
      expect(c.name.length).toBeGreaterThan(0);
      expect(c.country.length).toBeGreaterThan(0);
    }
  });

  it('都市名が重複しない', () => {
    const names = CITIES.map(c => c.name);
    expect(new Set(names).size).toBe(names.length);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npx vitest run src/data/cities.test.ts`
Expected: FAIL（`./cities` が存在せず import エラー）

- [ ] **Step 3: Write minimal implementation**

`frontend/src/data/cities.ts`:
```ts
export interface City {
  name: string; // 表示名（例: "東京"）
  country: string; // optgroup ラベル（例: "日本"）
  lat: number;
  lon: number;
}

export const CITIES: City[] = [
  { name: '東京', country: '日本', lat: 35.681, lon: 139.767 },
  { name: '名古屋', country: '日本', lat: 35.17, lon: 136.906 },
  { name: '大阪', country: '日本', lat: 34.694, lon: 135.5 },
  { name: '広島', country: '日本', lat: 34.39, lon: 132.46 },
  { name: '福岡', country: '日本', lat: 33.59, lon: 130.401 },
  { name: '仙台', country: '日本', lat: 38.268, lon: 140.872 },
  { name: '札幌', country: '日本', lat: 43.062, lon: 141.347 },
  { name: 'ソウル', country: '韓国', lat: 37.567, lon: 126.978 },
  { name: '釜山', country: '韓国', lat: 35.18, lon: 129.076 },
  { name: '台北', country: '台湾', lat: 25.033, lon: 121.565 },
  { name: '高雄', country: '台湾', lat: 22.627, lon: 120.301 },
  { name: '香港', country: '香港', lat: 22.32, lon: 114.17 },
];
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npx vitest run src/data/cities.test.ts`
Expected: PASS（4 テスト）

- [ ] **Step 5: Commit**

```bash
git add frontend/src/data/cities.ts frontend/src/data/cities.test.ts
git commit -m "feat: 主要都市の固定データ (cities.ts) を追加"
```

---

### Task 2: 都市選択 UI (`CityJumpSelect.tsx`)

**Files:**
- Create: `frontend/src/components/CityJumpSelect.tsx`
- Test: `frontend/src/components/CityJumpSelect.test.tsx`

**Interfaces:**
- Consumes: `City`, `CITIES`（Task 1）
- Produces:
  - `export interface CityJumpSelectProps { onSelect: (city: City) => void }`
  - `export const CityJumpSelect: React.MemoExoticComponent<(props: CityJumpSelectProps) => JSX.Element>`

**設計メモ:** select は controlled で常に `value=""`。onChange で `CITIES` から index を引き `onSelect(city)` を呼ぶ。value を state に持たないため同じ都市の連続選択でも onChange が発火する。optgroup は `country` の出現順でグルーピングする。

- [ ] **Step 1: Write the failing test**

`frontend/src/components/CityJumpSelect.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { CityJumpSelect } from './CityJumpSelect';

describe('CityJumpSelect', () => {
  it('都市を選ぶと対応する City で onSelect が呼ばれる', () => {
    const onSelect = vi.fn();
    render(<CityJumpSelect onSelect={onSelect} />);
    const select = screen.getByRole('combobox');
    fireEvent.change(select, { target: { value: '東京' } });
    expect(onSelect).toHaveBeenCalledTimes(1);
    expect(onSelect).toHaveBeenCalledWith(
      expect.objectContaining({ name: '東京', lat: 35.681, lon: 139.767 })
    );
  });

  it('プレースホルダ選択では onSelect が呼ばれない', () => {
    const onSelect = vi.fn();
    render(<CityJumpSelect onSelect={onSelect} />);
    const select = screen.getByRole('combobox');
    fireEvent.change(select, { target: { value: '' } });
    expect(onSelect).not.toHaveBeenCalled();
  });

  it('国ごとの optgroup を持つ', () => {
    render(<CityJumpSelect onSelect={vi.fn()} />);
    // 日本・韓国・台湾・香港 の 4 グループ
    const groups = document.querySelectorAll('optgroup');
    expect(groups).toHaveLength(4);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd frontend && npx vitest run src/components/CityJumpSelect.test.tsx`
Expected: FAIL（`./CityJumpSelect` が存在しない）

- [ ] **Step 3: Write minimal implementation**

`frontend/src/components/CityJumpSelect.tsx`:
```tsx
import { memo, useMemo, type ChangeEvent } from 'react';
import { CITIES, type City } from '../data/cities';

export interface CityJumpSelectProps {
  onSelect: (city: City) => void;
}

export const CityJumpSelect = memo(function CityJumpSelect({ onSelect }: CityJumpSelectProps) {
  // country の出現順を保ったグルーピング
  const grouped = useMemo(() => {
    const map = new Map<string, City[]>();
    for (const city of CITIES) {
      const list = map.get(city.country) ?? [];
      list.push(city);
      map.set(city.country, list);
    }
    return [...map.entries()];
  }, []);

  const handleChange = (e: ChangeEvent<HTMLSelectElement>) => {
    const name = e.target.value;
    if (!name) return;
    const city = CITIES.find(c => c.name === name);
    if (city) onSelect(city);
  };

  return (
    <div className="city-jump">
      <select
        className="city-jump-select"
        value=""
        onChange={handleChange}
        aria-label="主要都市へジャンプ"
      >
        <option value="">都市を選択…</option>
        {grouped.map(([country, cities]) => (
          <optgroup key={country} label={country}>
            {cities.map(city => (
              <option key={city.name} value={city.name}>
                {city.name}
              </option>
            ))}
          </optgroup>
        ))}
      </select>
    </div>
  );
});
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd frontend && npx vitest run src/components/CityJumpSelect.test.tsx`
Expected: PASS（3 テスト）

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/CityJumpSelect.tsx frontend/src/components/CityJumpSelect.test.tsx
git commit -m "feat: 都市ジャンプ用の select UI (CityJumpSelect) を追加"
```

---

### Task 3: 地図の flyTo 対応 (`MapView.tsx`)

**Files:**
- Modify: `frontend/src/components/MapView.tsx`

**Interfaces:**
- Consumes: react-leaflet の `useMap`（既に import 済み）
- Produces:
  - `MapViewProps` に `jumpTarget?: JumpTarget | null` を追加
  - `export type JumpTarget = { lat: number; lon: number; seq: number }`（App が参照）

**設計メモ:** `CityFlyTo` は `MapContainer` の子として置き `useMap()` で map を取得。`useEffect([map, target])` で `target` が非 null のとき `map.flyTo`。`seq` により同一座標の再選択でも target 参照が変わり再発火する。既存の `FocusAnimation`（URL フォーカス）とは併存し、初期表示時は `jumpTarget=null` なので競合しない。

- [ ] **Step 1: `JumpTarget` 型と定数を追加**

`MapView.tsx` の `FOCUS_TARGET_ZOOM = 16;`（16 行目付近）の下に追記:
```ts
// 都市ジャンプ時の共通ズーム
const CITY_JUMP_ZOOM = 14;

export type JumpTarget = { lat: number; lon: number; seq: number };
```

- [ ] **Step 2: `CityFlyTo` コンポーネントを追加**

既存の `FocusAnimation` コンポーネント定義（`const FocusAnimation = memo(...)` のブロック、122 行目付近の閉じ）の直後に追記:
```tsx
// サイドバーの都市選択で指定された座標へ flyTo するコンポーネント
const CityFlyTo = memo(function CityFlyTo({ target }: { target: JumpTarget | null }) {
  const map = useMap();

  useEffect(() => {
    if (!target) return;
    map.flyTo([target.lat, target.lon], CITY_JUMP_ZOOM, { animate: true, duration: 2 });
  }, [map, target]);

  return null;
});
```

- [ ] **Step 3: `MapViewProps` に prop を追加**

`interface MapViewProps { ... }`（153 行目付近）に追加:
```ts
  jumpTarget?: JumpTarget | null;
```

そして関数シグネチャの分割代入に `jumpTarget = null,` を追加:
```tsx
export const MapView = memo(function MapView({
  useMockData = true,
  filters,
  onLoadingChange,
  onDataChange,
  jumpTarget = null,
}: MapViewProps) {
```

- [ ] **Step 4: `MapContainer` 内に `CityFlyTo` を配置**

既存の `<MapEventsHandler onBoundsChange={handleBoundsChange} />`（258 行目付近）の直後に追記:
```tsx
        {/* 都市ジャンプ */}
        <CityFlyTo target={jumpTarget} />
```

- [ ] **Step 5: 型チェックとビルド確認**

Run: `cd frontend && npx tsc --noEmit`
Expected: エラーなし（exit 0）

- [ ] **Step 6: 既存テストが壊れていないこと**

Run: `cd frontend && npx vitest run`
Expected: PASS（Task 1・2 のテスト含め全て緑）

- [ ] **Step 7: Commit**

```bash
git add frontend/src/components/MapView.tsx
git commit -m "feat: MapView に都市ジャンプ (jumpTarget/CityFlyTo) を追加"
```

---

### Task 4: App 配線 (`App.tsx`)

**Files:**
- Modify: `frontend/src/App.tsx`

**Interfaces:**
- Consumes: `City`（Task 1）、`JumpTarget`・`MapView` の `jumpTarget` prop（Task 3）、`CityJumpSelect`（Task 2）
- Produces: なし（最終結線）

**設計メモ:** `jumpTarget` state を App が保持し、`CityJumpSelect.onSelect` で `seq` をインクリメント。`CityJumpSelect` はサイドバー内 `StatsDisplay` の上に置く。

- [ ] **Step 1: import を追加**

`App.tsx` 冒頭の import 群に追加:
```ts
import { CityJumpSelect } from './components/CityJumpSelect';
import type { City } from './data/cities';
import type { JumpTarget } from './components/MapView';
```

- [ ] **Step 2: `jumpTarget` state とハンドラを追加**

`const [isSidebarOpen, setIsSidebarOpen] = useState(false);` の直後に追加:
```ts
const [jumpTarget, setJumpTarget] = useState<JumpTarget | null>(null);

const handleCitySelect = useCallback((city: City) => {
  setJumpTarget(prev => ({ lat: city.lat, lon: city.lon, seq: (prev?.seq ?? 0) + 1 }));
}, []);
```

- [ ] **Step 3: サイドバーに `CityJumpSelect` を配置**

`<aside id="app-sidebar" ...>` 内、`<StatsDisplay ... />` の**直前**に追加:
```tsx
          {/* 都市ジャンプ */}
          <CityJumpSelect onSelect={handleCitySelect} />
```

- [ ] **Step 4: `MapView` に `jumpTarget` を渡す**

既存の `<MapView ... />` に prop を追加:
```tsx
          <MapView
            useMockData={false}
            filters={filterParams}
            onLoadingChange={setIsLoading}
            onDataChange={handleDataChange}
            jumpTarget={jumpTarget}
          />
```

- [ ] **Step 5: 型チェック**

Run: `cd frontend && npx tsc --noEmit`
Expected: エラーなし（exit 0）

- [ ] **Step 6: 全テスト実行**

Run: `cd frontend && npx vitest run`
Expected: PASS（全テスト緑）

- [ ] **Step 7: Commit**

```bash
git add frontend/src/App.tsx
git commit -m "feat: App に都市ジャンプを結線し MapView へ jumpTarget を渡す"
```

---

### Task 5: スタイルと手動確認

**Files:**
- Modify: `frontend/src/App.css`

**設計メモ:** `.city-jump-select` にサイドバー幅いっぱい・見やすい padding を与える。既存の CSS 変数・トーンに合わせる（`App.css` の既存 `.stats-display` 等を参照）。

- [ ] **Step 1: `.city-jump` / `.city-jump-select` の最小スタイルを追加**

`frontend/src/App.css` の末尾に追加（既存の余白・角丸トーンに合わせて調整）:
```css
.city-jump {
  padding: 8px 12px;
}

.city-jump-select {
  width: 100%;
  padding: 8px;
  font-size: 14px;
  border: 1px solid #ccc;
  border-radius: 6px;
  background: #fff;
}
```

- [ ] **Step 2: dev サーバで手動確認**

Run（フォアグラウンド）: `cd frontend && npm run dev`
確認項目:
1. サイドバー上部に「都市を選択…」ドロップダウンが出る。
2. 「福岡」を選ぶと地図が福岡へ滑らかに移動し、周辺の Y 字路マーカーが表示される。
3. 同じ「福岡」を再選択しても再び福岡へ寄る（seq 再発火）。
4. optgroup（日本/韓国/台湾/香港）が表示される。
確認後 Ctrl-C で停止。

- [ ] **Step 3: lint / 型 / テスト最終確認**

Run: `cd frontend && npm run lint && npx tsc --noEmit && npx vitest run`
Expected: すべて成功。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/App.css
git commit -m "style: 都市ジャンプ select のスタイルを追加"
```

---

## Self-Review

**Spec coverage:**
- 固定 12 都市リスト → Task 1 ✓
- サイドバー dropdown（optgroup） → Task 2 + Task 4 ✓
- flyTo（zoom 14 / duration 2） → Task 3 ✓
- 同一都市の再選択（seq） → Task 3・4 ✓
- bounds 連動で自動ロード → 既存ロジック流用（新規コードなし、Task 5 手動確認）✓
- URL フォーカスと非競合 → Task 3 設計メモ・`jumpTarget=null` 初期値 ✓
- バックエンド不変 → 全タスクがフロントのみ ✓
- テスト（cities / CityJumpSelect） → Task 1・2 ✓

**Placeholder scan:** なし（全コードブロック実体あり）。

**Type consistency:** `JumpTarget = {lat, lon, seq}` は Task 3 で定義し Task 4 が import。`City` は Task 1 定義、Task 2・4 が参照。`CityJumpSelectProps.onSelect(city: City)` は Task 2 定義・Task 4 使用で一致。`CITIES` は全参照で同名。
